import data from '../constants/data';

const initialState = {
  tags: data
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;